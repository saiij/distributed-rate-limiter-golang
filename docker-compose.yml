services:
  redis:
    image: redis:7-alpine

  app:
    build: .
    environment:
      - REDIS_ADDR=redis:6379
    depends_on:
      - redis

  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
